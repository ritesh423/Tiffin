# What I cut and why

The final cut list will be recorded here as the assignment is built. The current priority is completing the three required screens, reliable saved state, and clear submission documentation before adding visual polish.

# Tiffin

Tiffin is a small Jetpack Compose Android app for browsing daily tiffins from nearby home kitchens.

## Current status

The empty single-module Compose project is configured with Kotlin and minimum SDK 24.

## Build

From the repository root, run:

```bash
./gradlew assembleDebug
```

The debug APK is written to:

```text
app/build/outputs/apk/debug/app-debug.apk
```

